/*
package com.oo.demo;

import com.oo.onlyoffice.core.SaveFileProcessor;
import org.springframework.stereotype.Service;

import java.util.Map;

*/
/**
 * @BelongsProject: onlyoffice-demo
 * @BelongsPackage: com.oo.demo
 * @Author: TongHui
 * @CreateTime: 2023-08-01 15:50
 * @Description: TODO
 * @Version: 1.0
 *//*


@Service
public class demo implements SaveFileProcessor {

    @Override
    public void saveBeforeInitialization(byte[] bytes, String fileExtension) throws Exception {
        SaveFileProcessor.super.saveBeforeInitialization(bytes, fileExtension);
    }

    @Override
    public void save(Map<String, String> map, byte[] file, String key, String version) {
        SaveFileProcessor.super.save(map, file, key, version);
    }

    @Override
    public void save(Map<String, String> map, byte[] file, byte[] changes, String key, String version) {
        SaveFileProcessor.super.save(map, file, changes, key, version);
    }

    @Override
    public void saveAfterInitialization(byte[] bytes, String fileExtension) throws Exception {
        SaveFileProcessor.super.saveAfterInitialization(bytes, fileExtension);
    }
}
*/
